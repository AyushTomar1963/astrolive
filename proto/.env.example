# Leave empty for local `npm run dev`. Vite proxies /api to 127.0.0.1:8000.
# On Vercel, set this to the public FastAPI URL with no trailing slash.
# Example: https://astrolive-api.onrender.com
VITE_API_URL=
